#include "main.h"
#include <stdio.h>
/**
* print_alphabet - Make the alphabet
*
* Return:void
*/
int main()
{

    char ch = 'a';

    for (ch = 'a'; ch <= 'z'; ch++)
        putchar(ch);

    return (0);
}
